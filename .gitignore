# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Environments
.venv/

# Stray lockfile from ad-hoc `uv run --with ...` (Partwright ships no lockfile)
uv.lock

# Build / packaging artifacts
build/
dist/
*.egg-info/
.eggs/

# macOS
.DS_Store

# Saved sketches (output of `partwright sketch`)
*.svg

# Dogfooding harness output (scaffolded repos + reports)
dogfood/work/
