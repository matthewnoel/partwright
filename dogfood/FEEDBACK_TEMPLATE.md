# Dogfood feedback — <part name>

Copy this file into your scaffolded repo as `DOGFOOD_FEEDBACK.md` and fill it in
as you build. `dogfood/run.py --collect` aggregates every copy into one report.

Be specific and concrete: a friction point with the exact command, file, or line
is worth ten vague impressions. You are the test; the tool is what's on trial.

## What I built
<one line: the part and whether you got it to render correctly>

## Friction with the tool
<where Partwright itself (the CLI, the scaffold output, the CLAUDE.md, the brief
schema) slowed you down, surprised you, or made you guess. Distinguish "the part
was hard" from "the tool was hard" — only the second kind belongs here.>

## What the scaffold got right
<what saved you time or kept you on the rails — keep the good parts honest too>

## Missing / wanted
<a flag, a template field, a doc, a guardrail you reached for and it wasn't there>

## Did the visual loop work?
<did generate.py -> preview.py -> reading preview.png actually let you verify the
geometry? where did it fall short?>

## Severity
<blocker | major | minor | nit> — <one line on impact>
